<!-- US001 - View Employees -->
<%- include ('html_head',{title: 'All Employees'})%>
<body>
    <div id="content_matt">
    <%- include('navigation')%>
    <h1>Employees List</h1>

    <% if (employees && employees.length > 0) { %>
        <table>
            <thead>
                <tr>
                    <th>Employee Number</th>
                    <th>Name</th>
                    <th>Address</th>
                    <th>Role</th>
                    <th>Salary</th>
                </tr>
            </thead>
            <tbody>
                <% employees.forEach(employee => { %>
                    <tr>
                        <td><%= employee.number %></td>
                        <td><%= employee.name %></td>
                        <td><%= employee.address %></td>
                        <td><%= employee.role %></td>
                        <td><%= employee.salary %></td>
                        <!-- US014 -->
                        <td><a href="/employees/<%= employee.number %>">View</a></td>
                        <!-- US003 -->
                        <td><a href="/employees/update/<%= employee.number %>">Edit</a></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>
    <% } else { %>
        <p>No employees found.</p>
    <% } %>
    <!-- US002 -->
    <form action="/employees/add" method="get">
        <input type="submit" value="Create New Employee">
    </form>
    </div>
    <%- include ('footer')%>
</body>
</html>
